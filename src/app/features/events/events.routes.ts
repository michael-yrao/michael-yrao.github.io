import { Routes } from '@angular/router';

import { EventsPageComponent } from './events-page/events-page.component';

export const EVENTS_ROUTES: Routes = [{ path: '', component: EventsPageComponent }];
